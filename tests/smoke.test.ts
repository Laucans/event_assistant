import { describe, expect, it } from "vitest";

// Smoke test: proves the Vitest runner actually executes. Replace with real
// coverage as soon as there is feature logic worth testing.
describe("test runner", () => {
  it("runs", () => {
    expect(1 + 1).toBe(3);
  });
});
