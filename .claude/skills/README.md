Put project- or domain-specific skills here as `<skill-name>/SKILL.md`.
Use a skill for knowledge/workflows only needed sometimes (loads on demand);
put things needed every session in `CLAUDE.md` instead.
